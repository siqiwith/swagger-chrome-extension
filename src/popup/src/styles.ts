import { createStyles } from '@material-ui/core';

const styles = () => createStyles({
  app: {
    margin: 10
  },

  openSwaggerButton: {
    width: 300
  },

  yamlSelector: {
    width: 300
  }
});

export default styles;