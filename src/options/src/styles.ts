import { createStyles } from '@material-ui/core';

const styles = () => createStyles({
  app: {
    margin: 10
  },

  container: {
    display: 'flex',
    flexWrap: 'wrap'
  }
});

export default styles;